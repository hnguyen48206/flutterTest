import 'package:flutter/material.dart';

class VerifyNumberUI extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
        child: Center(
          child: Text('VerifyNumberPage'),
        ),
        color: Colors.white);
  }
}
